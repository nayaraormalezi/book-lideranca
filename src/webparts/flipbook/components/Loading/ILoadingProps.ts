export interface ILoadingProps {
  /** Mensagem principal exibida abaixo do spinner. */
  label?: string;
  /** Cor de destaque usada no spinner (normalmente `primaryColor` do tema configurado). */
  accentColor?: string;
}
